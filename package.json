{
  "name": "acv-decision-tree",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npx expo start",
    "dev:web": "npx expo start --web",
    "build:web": "npx expo export --platform web",
    "deploy:web": "npm run build:web && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "expo": "~54.0.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-native": "0.76.6",
    "react-native-web": "0.19.10",
    "expo-status-bar": "~1.12.1"
  }
}
